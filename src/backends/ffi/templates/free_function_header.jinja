{%- if doc_comment %}
{{ doc_comment }}
{% endif -%}
{% if allow_clippy -%}
#[allow({{ allow_clippy }})]
{% endif -%}
{% if source_cfg -%}
#[cfg({{ source_cfg }})]
{% endif -%}
#[unsafe(no_mangle)]
pub unsafe extern "C" fn {{ fn_name }}(
{% for param in params -%}
{{ param }}{% if not loop.last %},
{% endif %}
{% endfor -%}
){% if return_type %} -> {{ return_type }}{% endif %} {
    clear_last_error();
{% if return_len_key %}
    set_last_return_len("{{ return_len_key }}", 0);
{% endif %}
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
