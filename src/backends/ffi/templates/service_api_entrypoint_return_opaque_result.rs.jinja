    match {{ call }} {
        Ok(value) => match insert_handle(value) {
            Ok(handle) => handle,
            Err(error) => { set_handle_error(&error); 0 }
        },
        Err(_) => 0,
    }
