    match {{ call }} {
        Ok(value) => match insert_handle(value) {
            Ok(handle) => handle,
            Err(error) => { set_handle_error(&error); 0 }
        },
        Err(error) => { set_last_error(2, &error.to_string()); 0 }
    }
