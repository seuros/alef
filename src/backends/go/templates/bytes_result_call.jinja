	var outPtr *C.uint8_t
	var outLen, outCap C.uintptr_t
	rc := {{ c_call }}
	if rc != 0 {
		return nil, lastError()
	}
	if outPtr == nil {
		return nil, lastError()
	}
	defer C.{{ ffi_prefix }}_free_bytes(outPtr, outLen, outCap)
	result := C.GoBytes(unsafe.Pointer(outPtr), C.int(outLen))
	return result, nil
