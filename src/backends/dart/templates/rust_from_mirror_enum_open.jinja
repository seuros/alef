{# Same asymmetry as rust_from_core_enum_open.jinja: the mirror enum declaration stays #}
{# unconditional, but this impl names `core_ty` (the host path) directly, so a whole-enum #}
{# gate (`source_cfg` from `en.cfg`) must carry through here too. #}
{% if source_cfg %}#[cfg({{ source_cfg }})]
{% endif %}impl From<{{ name }}> for {{ core_ty }} {
    fn from(v: {{ name }}) -> Self {
        match v {
