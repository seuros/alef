{% if cfg %}            #[cfg({{ cfg }})]
{% endif %}            {{ core_path }}::{{ vname }}(_0) => Self { {{ disc_ident }}: "{{ wire }}".to_string(), {{ fname }}: Some({{ expr }}), ..Default::default() },
