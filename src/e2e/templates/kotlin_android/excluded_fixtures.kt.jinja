package {{ package_name }}.e2e

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

class ExcludedBindingsTest {
{% for entry in entries %}
    @Test
    @Disabled("{{ entry.reason }}")
    fun {{ entry.name }}() {}
{% endfor %}
}
